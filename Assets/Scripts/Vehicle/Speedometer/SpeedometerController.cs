using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpeedometerController : MonoBehaviour {


	
	// Update is called once per frame
	void Update () {
        /*
        float translation = Input.GetAxis("Vertical") * speed;
        float rotation = Input.GetAxis("Horizontal") * rotationspeed;
        translation *= Time.deltaTime;
        rotation *= Time.deltaTime;
        rb.AddForce(this.thansform.forward * transation * 40);
        rb.AddTorque(this.transform.up * rotatin * 15);

        AnalogueSpeedConverter.ShowSpeed(rb.velocity.magnitude, 0, 100); // this calls the showspeed fonction on the analoguespeedconverter and checks the vector lenght 
        //*/


    }
}
